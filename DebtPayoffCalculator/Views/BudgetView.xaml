<UserControl x:Class="DebtPayoffCalculator.Views.BudgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" 
                      Text="Budget Management"
                      FontSize="32"
                      FontWeight="Bold"
                      Foreground="{StaticResource DarkBlueBrush}"
                      Margin="0,0,0,20"/>

        <!-- Summary Cards -->
        <UniformGrid Grid.Row="1" 
                    Rows="1" 
                    Columns="3"
                    Margin="0,0,0,20">

            <Border Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="20"
                    Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="💰 Total Budgeted"
                              FontSize="14"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalBudgeted, StringFormat=C0}"
                              FontSize="28"
                              FontWeight="Bold"
                              Foreground="{StaticResource PrimaryBlueBrush}"
                              Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="20"
                    Margin="5,0">
                <StackPanel>
                    <TextBlock Text="💸 Total Spent"
                              FontSize="14"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalSpent, StringFormat=C0}"
                              FontSize="28"
                              FontWeight="Bold"
                              Foreground="#F44336"
                              Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="20"
                    Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="📊 Remaining"
                              FontSize="14"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Remaining, StringFormat=C0}"
                              FontSize="28"
                              FontWeight="Bold"
                              Foreground="#4CAF50"
                              Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Add Category -->
        <Border Grid.Row="2"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="2"
                CornerRadius="10"
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Label Content="Add Custom Category:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource ModernTextBoxStyle}"
                        Width="200"
                        Margin="0,0,10,0"/>
                <Button Content="Add Category"
                       Command="{Binding AddCategoryCommand}"
                       Style="{StaticResource ModernButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Budget Categories -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Edit Budgets -->
            <Border Grid.Column="0"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="20"
                    Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Set Budget Amounts"
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="{StaticResource DarkBlueBrush}"
                              Margin="0,0,0,15"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="150"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" 
                                                  Text="{Binding Name}"
                                                  FontSize="14"
                                                  VerticalAlignment="Center"/>

                                        <TextBox Grid.Column="1"
                                                Text="{Binding BudgetedAmount, TargetNullValue=''}"
                                                Style="{StaticResource ModernTextBoxStyle}"
                                                Margin="10,0"
                                                GotKeyboardFocus="NumericTextBox_GotKeyboardFocus"
                                                PreviewMouseLeftButtonDown="NumericTextBox_PreviewMouseLeftButtonDown"/>

                                        <Button Grid.Column="2"
                                               Content="Delete"
                                               Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                               CommandParameter="{Binding}"
                                               Visibility="{Binding IsCustom, Converter={StaticResource BoolToVisibilityConverter}}"
                                               Background="#E53935"
                                               Foreground="White"
                                               BorderThickness="0"
                                               Padding="10,5"
                                               Cursor="Hand"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Button Content="Save Budget"
                           Command="{Binding SaveBudgetCommand}"
                           Style="{StaticResource ModernButtonStyle}"
                           HorizontalAlignment="Right"
                           Margin="0,20,0,0"/>
                </StackPanel>
            </Border>

            <!-- Budget Comparison -->
            <Border Grid.Column="1"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="10"
                    Padding="20"
                    Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Budget vs Actual (This Month)"
                              FontSize="18"
                              FontWeight="Bold"
                              Foreground="{StaticResource DarkBlueBrush}"
                              Margin="0,0,0,15"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                        <ItemsControl ItemsSource="{Binding BudgetComparisons}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                           BorderThickness="0,0,0,1"
                                           Padding="0,10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding CategoryName}"
                                                      FontWeight="SemiBold"
                                                      FontSize="14"
                                                      Margin="0,0,0,5"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock>
                                                        <Run Text="Budget: "/>
                                                        <Run Text="{Binding Budgeted, StringFormat=C0}" FontWeight="SemiBold"/>
                                                    </TextBlock>
                                                    <TextBlock>
                                                        <Run Text="Spent: "/>
                                                        <Run Text="{Binding Spent, StringFormat=C0}" FontWeight="SemiBold"/>
                                                    </TextBlock>
                                                    <TextBlock>
                                                        <Run Text="Remaining: "/>
                                                        <Run Text="{Binding Remaining, StringFormat=C0}" FontWeight="SemiBold"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1"
                                                          FontSize="24"
                                                          FontWeight="Bold"
                                                          Foreground="{StaticResource PrimaryBlueBrush}"
                                                          VerticalAlignment="Center">
                                                    <TextBlock.Text>
                                                        <Binding Path="PercentUsed" StringFormat="{}{0:N0}%"/>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
    </ScrollViewer>
</UserControl>
