<UserControl x:Class="PersonalFinancePlanner.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" 
                      Text="Financial Dashboard"
                      FontSize="32"
                      FontWeight="Bold"
                      Foreground="{StaticResource DarkBlueBrush}"
                      Margin="0,0,0,30"/>

            <!-- Summary Cards -->
            <UniformGrid Grid.Row="1" 
                        Rows="1" 
                        Columns="5"
                        Margin="0,0,0,30">

                <!-- Monthly Income Card -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="💵 Monthly Income"
                                  FontSize="14"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding MonthlyIncome, StringFormat=C0}"
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="#4CAF50"
                                  Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Monthly Expenses Card -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="💸 Monthly Expenses"
                                  FontSize="14"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding MonthlyExpenses, StringFormat=C0}"
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="#F44336"
                                  Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Budget Remaining Card -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="📊 Budget Remaining"
                                  FontSize="14"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding BudgetRemaining, StringFormat=C0}"
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="{StaticResource PrimaryBlueBrush}"
                                  Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Current Savings Card -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="🏦 Current Savings"
                                  FontSize="14"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding CurrentSavings, StringFormat=C0}"
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="#2196F3"
                                  Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Total Debt Card -->
                <Border Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="💰 Total Debt"
                                  FontSize="14"
                                  Foreground="{StaticResource TextSecondaryBrush}"
                                  FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalDebt, StringFormat=C0}"
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="#FF9800"
                                  Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Charts Section -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Spending by Category Chart -->
                <Border Grid.Column="0"
                        Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Spending by Category (This Month)"
                                  FontSize="18"
                                  FontWeight="Bold"
                                  Foreground="{StaticResource DarkBlueBrush}"
                                  Margin="0,0,0,15"/>
                        <lvc:PieChart Series="{Binding SpendingByCategorySeries}"
                                     Height="350"
                                     LegendPosition="Right"/>
                    </StackPanel>
                </Border>

                <!-- Monthly Trend Chart -->
                <Border Grid.Column="1"
                        Background="{StaticResource CardBackgroundBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="20"
                        Margin="15,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Income vs Expenses (Last 6 Months)"
                                  FontSize="18"
                                  FontWeight="Bold"
                                  Foreground="{StaticResource DarkBlueBrush}"
                                  Margin="0,0,0,15"/>
                        <lvc:CartesianChart Series="{Binding MonthlyTrendSeries}"
                                           YAxes="{Binding MonthlyTrendYAxes}"
                                           XAxes="{Binding MonthlyTrendXAxes}"
                                           Height="350"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>

